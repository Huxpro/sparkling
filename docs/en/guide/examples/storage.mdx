# Storage

Sparkling provides a key–value storage API backed by native persistence. This
demo sets, reads, and removes items.

:::tip
Storage calls the native bridge — use the **QR code** tab to persist data on a
real device.
:::

<Go
  example="sparkling-go"
  defaultEntryName="storage-demo"
  defaultFile="src/pages/storage-demo/App.tsx"
  entry="src/pages/storage-demo"
/>
